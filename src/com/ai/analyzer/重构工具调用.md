# 重构工具调用

我发现我一直以来在犯一个坑爹的错误，给AGent所有的burp工具调用的逻辑，几乎都是基于mcp的
其实所有工具都应该像 /Tools/BurpExtTools.java 一样，直接调用 montaya api去调用就可以了，完全不需要像个傻瓜一样调用mcp还非要绕一圈
接下来你的任务就是将以前所有调用 Burp MCP的逻辑改了，把mcp工具全部改成 @Tool 来调用

/mcpClient 文件夹下的逻辑，全部重构，换成Tools的逻辑

相应的提示词也要全部修改，变量名函数名也要改成对应正确的名字

mcp server端的源码在 /mcp-server 中